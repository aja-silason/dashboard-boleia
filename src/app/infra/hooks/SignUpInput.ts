export type SignUpInput = {
    firstName: string,
    lastName: string,
    phoneNumber: string,
    isDriver: boolean,
    identificationNumber: string,
    licenseNumber: string,
    password: string
}